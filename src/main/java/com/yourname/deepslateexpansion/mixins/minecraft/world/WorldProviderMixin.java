package com.yourname.deepslateexpansion.mixins.minecraft.world;

import net.minecraft.world.WorldProvider;
import org.spongepowered.asm.mixin.Mixin;

/**
 * This mixin is kept as a placeholder. The actual world height expansion
 * is handled by WorldMixin. We keep this file so the project structure
 * remains intact for future improvements.
 */
@Mixin(WorldProvider.class)
public abstract class WorldProviderMixin {
    // No changes needed – the default height is fine.
}
